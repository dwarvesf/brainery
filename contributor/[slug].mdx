---
title: "{contributorName}'s Profile"
description: "Contributions by {contributorName}"
---
# {contributorName}

### Contributions

<ul>
{contributorMemos?.map(memo => (
    <li key={memo.file_path}>
        <MemoLink filePath={memo.file_path}>
            {memo.title}
        </MemoLink>
    </li>
))}
</ul>