---
title: "Contributors"
description: "List of contributors"
---

# Contributors

<ul>
    {contributors.map(contributor => (
    <li key={contributor}>
        <Link
        href={`/contributor/${contributor}`}
        className="text-lg"
        >
        {contributor}
        </Link>
    </li>
    ))}
</ul>