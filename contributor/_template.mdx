---
skip_frontmatter_check: true
---

# {contributorName}

### Contributions

<ul>
{contributorMemos?.map(memo => (
    <li key={memo.file_path}>
        <MemoLink filePath={memo.file_path}>
            {memo.title}
        </MemoLink>
    </li>
))}
</ul>